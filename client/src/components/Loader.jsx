import { CircularProgress } from "@material-ui/core";

const Loader = () => {
    return (
        <div className="table-container">
            <CircularProgress />
        </div>
    )
};

export default Loader;